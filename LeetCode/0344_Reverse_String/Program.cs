namespace _0344_Reverse_String
{
    internal class Program
    {
        static void Main(string[] args)
        {
            /*
            char[] s = { 'h', 'e', 'l', 'l', 'o' };
            for (int i = 0, j = s.Length - 1; i < s.Length / 2; i++, j--)
            {
                char temp = s[i];
                s[i] = s[j];
                s[j] = temp;
            }
            */
        }
    }
}